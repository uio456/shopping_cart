<% if product.is_favorited?(current_user) %>
  <%= link_to 'Unfavorite', unfavorite_product_path(product), method: :post, class: "btn btn-xs btn-info" %>
<% else %>  
  <%= link_to 'Favorite', favorite_product_path(product), method: :post, class: "btn btn-xs btn-primary" %>
<% end %>