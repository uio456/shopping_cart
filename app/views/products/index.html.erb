<h1>商品列表</h1>
<span class='pull-right'><%= cart_item_discount_price %></span>
<!-- 寫個 helper 如果折扣是 0，寫些其他字，可輪流播放些好笑的字，etc:快點買、不要只會看、要考慮的是買幾件不是買不買  -->

<%#= link_to '新增商品', new_product_path, class: "btn btn-default btn-primary" %>
 <h3>
  <%= link_to cart_path do %>
    <%= current_cart.count_items %>
    <i "<span class= 'glyphicon glyphicon-shopping-cart'>Cart</span></i>
  <% end %>
</h3>

<table class="table table-hover">
  <thead>
    <tr>
      <td>商品名稱</td>
      <td>售價</td>
      <td>供應商</td>
      <td>商品狀態</td>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
    <tr>
      <td>
        <%= link_to '放到購物車', add_cart_path(product), method: "post", class: "btn btn-xs btn-danger" %>
        <%= product.title %>
      </td>
      <td><%= product.price %></td>
      <td>
        <%= product.vendor.name %>
      </td>
      <td>
        <%= product_state(product) %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>